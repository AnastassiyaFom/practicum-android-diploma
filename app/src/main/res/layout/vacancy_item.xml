<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/padding_9"
    android:paddingBottom="@dimen/padding_9">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:paddingEnd="@dimen/padding_12"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/logo_placeholder" />

    <TextView
        android:id="@+id/vacancy_name_and_area"
        style="@style/TextAppearance.Medium22"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Android-разработчик, Москва" />

    <TextView
        android:id="@+id/employer_name"
        style="@style/TextAppearance.Regular16"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/logo"
        app:layout_constraintTop_toBottomOf="@+id/vacancy_name_and_area"
        tools:text="Еда" />

    <TextView
        android:id="@+id/salary"
        style="@style/TextAppearance.Regular16"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/logo"
        app:layout_constraintTop_toBottomOf="@+id/employer_name"
        tools:text="от 100 000 P" />


</androidx.constraintlayout.widget.ConstraintLayout>
